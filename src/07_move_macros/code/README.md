# 第七章 · 示例代码

整章共用一个包 **`macro_lab/`**（`Move.toml` 中 `name = "ch07_macro_lab"`），便于对照各节宏用法。

| 节 | 目录 / 模块 | 说明 |
|----|----------------|------|
| 7.3–7.6 | `sources/demo.move` | 自定义 `macro fun`、`vector` 宏示例与测试 |

构建：

```bash
cd macro_lab && sui move build
```
